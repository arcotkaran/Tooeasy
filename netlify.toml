# Git-connected deploys: Netlify detects Next.js and injects its own runtime,
# which sets the build command, publish directory and SSR function. Declaring
# any of those here fights the runtime, so we only pin the Node version.
[build.environment]
  NODE_VERSION = "22"
