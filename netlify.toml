# Netlify detects Next.js and injects its own runtime, which chooses the
# publish directory and creates the SSR function. We set only the build
# command and Node version — declaring the plugin or the publish directory
# here conflicts with the injected runtime and fails the build.
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
