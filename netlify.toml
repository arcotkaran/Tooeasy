[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# The app is server-rendered now (logins, roles, database), so it needs the
# Next.js runtime rather than a static publish.
[[plugins]]
  package = "@netlify/plugin-nextjs"
