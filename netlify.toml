# Netlify auto-detects Next.js and injects its own runtime, which handles the
# build command, publish directory and SSR functions. Declaring those here
# fights the runtime, so we only pin the Node version.
[build.environment]
  NODE_VERSION = "22"
